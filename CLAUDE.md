# CLAUDE.md

このリポジトリで作業する際のルール。

## プロジェクト概要

- `backlog.html` … HTML ファイル 1 つで動作するタスクリストアプリ (看板 / テーブル)。外部依存なし。
- データはファイル内の `<script type="application/json" id="backlog-data">` に埋め込まれ、
  「上書き保存」「HTML をダウンロード」で自身の `<style id="backlog-style">` / `<script id="backlog-script">` から HTML を再生成する。
- CSS は `.backlog-root` 配下にスコープし、SharePoint などの他ページへのインライン埋め込みでも動作させる。
- 詳細は `README.md` を参照。

## 作業ルール

### 指示はルールとしてドキュメント化する

- セッション内でユーザーから指示された内容のうち、他のセッションでも追従が必要なもの
  (仕様の決定事項、UI / 挙動の方針、作業の進め方など) は、その都度この `CLAUDE.md` にルールとして追記する。
- 一度きりの作業指示はルール化しない。「今後もこうする」と読める指示はルール化する。
- ルール化した内容はコミットに含める。

### 作業完了後は HTML ファイルを確認できるようにする

- `backlog.html` を変更した作業が完了したら、必ず最終版の `backlog.html` をユーザーに送付する
  (SendUserFile などでファイルをそのまま渡す。パスを伝えるだけで済ませない)。
- 送付前に Chromium (Playwright) などで実際に開き、エラーなく描画されることと、
  ダウンロード / 保存で生成した HTML を再度開いてデータが復元されること (ラウンドトリップ) を確認する。
- 見た目に関わる変更はスクリーンショットも添える。

### 適宜コミット・プッシュする

- 意味のある単位の変更が完了するたびにコミットし、指定された作業ブランチへプッシュする。
  作業の最後にまとめて 1 回ではなく、区切りごとに行う。
- **1 機能 1 コミット** を可能な限り維持する。複数の作業を一度に依頼された場合も、機能ごとに実装・確認・コミットを繰り返し、
  1 つのコミットに複数の機能を混ぜない (ドキュメントの追記など、その機能に付随する変更は同じコミットに含めてよい)。
- コミットメッセージは変更内容が分かる説明的なものにする。
- プッシュ先は指示された作業ブランチのみ。他のブランチへは指示なしにプッシュしない。
- プルリクエストは明示的に依頼された場合のみ作成する。

## プロダクト仕様の決定事項

### 基本

- アプリ名 (プロジェクト名の既定値) は **Backlog.html**。アクセントカラーは **#A100FF**。
- 配置先は SharePoint を想定。HTML 単体でも、他ページへのインライン埋め込みでも動作させる。
- 更新の反映は「⬇ HTML をダウンロード」が主。ローカル (file://) の Chrome / Edge では「上書き保存」も出す。
- データのみの JSON / HTML / CSV の import / export を用意し、ベース HTML (アプリ本体) の更新に追従できるようにする。
- 未保存の変更があるときは、ヘッダーに「未保存 (n)」バッジを出し、クリックで変更内容をモーダル表示する。
  リロードやページ移動時には `beforeunload` で警告する。

### レーン

- 標準レーンは **TODO / IN PROGRESS / IN REVIEW / DONE** (DONE は完了扱い)。
- レーンはカスタムで追加・並べ替え・削除できる。
- 先頭レーンから他レーンへ移動すると着手日、完了扱いレーンへ移動すると完了日を自動入力する (未入力時のみ)。

### タスクの標準項目

- タイトル / サマリー / 概要 / 完了予定日 / 着手予定日 / 着手日 / 完了日。
- これに加えて レーン / 担当者 / 優先度 / マイルストーン / タグ / 更新履歴 を持つ。
- 各タスクに更新履歴 (いつ・誰が・何を変更したか) を自動記録する。操作者名は localStorage に保存する。

### マスタ

- メンバー: 一覧で複数選択して一括削除、テキストエリアから一括追加できる UI を用意する。
- タグ: ラフに扱う。タスクの編集ダイアログやテーブルからその場で新規作成・付与できる。
- マイルストーン: 標準は「なし」(空)。必要に応じて設定から追加する。
- カスタム項目: プロジェクトごとに複数追加できる。型は text / long text / date / list。標準は「なし」(空)。
  タスク編集ダイアログでは標準項目と同じグリッドに並べて表示し、セクションを分けない。

### テーブル View

- 列ごとの絞り込み (フィルタ行) に対応する。
- 列の表示 / 非表示、絞り込み、並び順のセットを「ビュー」として名前を付けて保存できる (データファイルに保存)。

### 設定モーダル

- 高さは固定 (タブを切り替えても高さが変わらないようにする)。

## 実装上の注意

- 単一ファイル構成を維持する。外部 CSS / JS / CDN への依存を追加しない。
- `<script id="backlog-script">` 内に `</script>` という文字列を直接書かない (HTML パーサが終了タグと解釈する)。
  文字列で必要な場合は `'<' + '/script>'` のように分割する。正規表現でも `<\/script` のように書く。
- 埋め込む JSON は `<` を `\u003c` にエスケープして出力する (`dataJson()` を経由する)。
- 新しい CSS ルールはすべて `.backlog-root { ... }` のネスト内に書き、`body` / `html` / `:root` を直接スタイルしない。
- `document.title` や `document.body` など、ホストページに影響するものは単体ファイル (`data-standalone`) の場合のみ操作する。
- DOM の取得は `root` (= `#backlog-app`) を基点にし、`document.getElementById` などページ全体への問い合わせを避ける。
- タスクを変更する処理は `snapshot()` → 変更 → `recordDiff()` の順で書き、更新履歴が残るようにする。
- データ構造を変えるときは `normalize()` で旧データからの移行 (既定値の補完) を行い、`DATA_VERSION` を上げる。
- 変更後は Playwright (Chromium) で動作確認する。手順は `README.md` の「動作確認」を参照。
