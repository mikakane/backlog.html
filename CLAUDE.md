# CLAUDE.md

このリポジトリで作業する際のルール。

## プロジェクト概要

- `backlog.html` … HTML ファイル 1 つで動作するタスクリストアプリ (看板 / テーブル)。外部依存なし。
- データはファイル内の `<script type="application/json" id="backlog-data">` に埋め込まれ、
  「上書き保存」「HTML をダウンロード」で自身の `<style id="backlog-style">` / `<script id="backlog-script">` から HTML を再生成する。
- CSS は `.backlog-root` 配下にスコープし、他ページへのインライン埋め込みでも動作させる。
- 詳細は `README.md` を参照。

## 作業ルール

### 指示はルールとしてドキュメント化する

- セッション内でユーザーから指示された内容のうち、他のセッションでも追従が必要なもの
  (仕様の決定事項、UI / 挙動の方針、作業の進め方など) は、その都度この `CLAUDE.md` にルールとして追記する。
- 一度きりの作業指示はルール化しない。「今後もこうする」と読める指示はルール化する。
- ルール化した内容はコミットに含める。

### 作業完了後は HTML ファイルを確認できるようにする

- `backlog.html` を変更した作業が完了したら、必ず最終版の `backlog.html` をユーザーに送付する
  (SendUserFile などでファイルをそのまま渡す。パスを伝えるだけで済ませない)。
- 送付前に Chromium (Playwright) などで実際に開き、エラーなく描画されることと、
  ダウンロード / 保存で生成した HTML を再度開いてデータが復元されること (ラウンドトリップ) を確認する。
- 見た目に関わる変更はスクリーンショットも添える。

### 適宜コミット・プッシュする

- 意味のある単位の変更が完了するたびにコミットし、指定された作業ブランチへプッシュする。
  作業の最後にまとめて 1 回ではなく、区切りごとに行う。
- **1 機能 1 コミット** を可能な限り維持する。複数の作業を一度に依頼された場合も、機能ごとに実装・確認・コミットを繰り返し、
  1 つのコミットに複数の機能を混ぜない (ドキュメントの追記など、その機能に付随する変更は同じコミットに含めてよい)。
- コミットメッセージは変更内容が分かる説明的なものにする。
- プッシュ先は指示された作業ブランチのみ。他のブランチへは指示なしにプッシュしない。
- プルリクエストは明示的に依頼された場合のみ作成する。

## プロダクト仕様の決定事項

### 基本

- アプリ名 (プロジェクト名の既定値) は **Backlog.html**。アクセントカラーは **#A100FF**。
- コンセプトメッセージは「**Believe it or not, this is "The Only Excel Thing to Do".**」。README の冒頭に引用として掲げる。
- README 冒頭の「想定シチュエーション」: チームで使えるタスク管理ツールが導入できず Excel でタスク管理を続けている /
  一つの Excel に何でも突っ込んで閲覧性・保守性が悪い、という状況向け。
  「タスク管理ツールが使えるなら、絶対タスク管理ツールを使ったほうがいい」という立場を明記する。
- デザインは **ボーダーレス**。要素の区切りは 1px の罫線ではなく、背景のトーン差 (`--field` / `--row-alt` / `--lane-bg`) と
  わずかな影 (`--shadow`) で表現する。入力欄・ボタンも枠線なし (塗りつぶし + フォーカスリング)。
- 配置先は SharePoint などのクラウドストレージや共有フォルダを前提にする (SharePoint に限定しない)。ドキュメント上の表現も
  「SharePoint などのクラウドストレージ上に配置して動作させることを前提」程度にとどめ、SharePoint 固有の注意は小見出しに分ける。
  HTML 単体でも、他ページへのインライン埋め込みでも動作させる。
- 更新の反映は「⬇ HTML をダウンロード」が主。ローカル (file://) の Chrome / Edge では「上書き保存」も出す。
- データのみの JSON / HTML / CSV の import / export を用意し、ベース HTML (アプリ本体) の更新に追従できるようにする。
  JSON / HTML (バックアップ・移行、全置換) と CSV (タスク一覧の更新 / 追加) は用途が根本的に違うので、セクションも UI (ボタン・入力欄) も分離する。
  CSV は LIST View のツールバーからも読み込み / 書き出しできる (書き出しは絞り込み中の行)。
- 未保存の変更があるときは、ヘッダーに「未保存 (n)」バッジを出し、クリックで変更内容をモーダル表示する。
  リロードやページ移動時には `beforeunload` で警告する。

### AI にタスク操作を任せる

- HTML ファイル自体を AI に渡してタスク操作 (追加・更新・削除、マスタ変更) を依頼できるようにする。
  ファイル先頭に AI 向けの指示コメント (`AI_GUIDE_LINES`) を埋め込み、再生成時にも同じ内容を出力する。
- 指示の要点: 通常のタスク操作では `id="backlog-data"` の JSON だけを編集し、`<style>` / `<script>` などアプリ本体は
  明示的に指示されない限り 1 文字も変更しない。データ編集の約束事 (id / no / updatedAt / history / 日付の自動補完 / savedAt・hash) も記載する。
- HTML コメント内には `--` と `</` を含めない。HTML を読み込む処理はコメントを除去してからデータブロックを探す。

### フッター

- 画面下部のフッターに、ファイルの保存先・最終更新日時・データのハッシュを常に表示する。
  最終更新日時とハッシュは保存 / ダウンロード時にデータへ埋め込む (`savedAt` / `hash`)。ハッシュは `savedAt` / `hash` を除いたデータから計算し、
  内容が同じなら同じ値になる。未保存の変更があるときは現在のハッシュも併記する。

### 検索

- ヘッダーの検索はテキスト入力 1 つだけにし、`キー:値` の DSL で絞り込む (セレクトボックスは置かない)。
  キーは日本語 / 英語 (`担当:` / `assignee:` など、`QUERY_KEYS`)、カスタム項目は項目名がキー。空白区切りは AND、`-` で除外、`"..."` で空白を含む値。
  日付は `なし` / `あり` / `今日` / `超過` / `<=YYYY-MM-DD` / `A..B` / 前方一致に対応する。
- 検索欄の「?」で構文ヘルプのモーダルを出す (例をクリックで入力)。
- 旧形式のビュー (セレクトによる filter) は `normalize()` で DSL に変換する。

### 表記

- ビュー切り替えの表記は **KANBAN** (看板) / **LIST** (テーブル)。ドキュメントやコード内の説明では「KANBAN (看板)」「LIST (テーブル)」と併記してよい。

### レーン

- 標準レーンは **TODO / IN PROGRESS / IN REVIEW / DONE** (DONE は完了扱い)。
- レーンはカスタムで追加・並べ替え・削除できる。
- 先頭レーンから他レーンへ移動すると着手日、完了扱いレーンへ移動すると完了日を自動入力する (未入力時のみ)。

### KANBAN (看板) View

- ボード上部の「カードの表示項目…」で、カードに出す項目 (番号 / 優先度 / サマリー / タグ / マイルストーン / 期日 / 担当者 / 各カスタム項目) を
  オン・オフできる。設定はデータファイル (`cardFields`、カスタム項目は `fields[].showOnCard`) に保存する。タイトルは常に表示。

### タスクの追加

- タイトルだけで雑に追加できるクイック追加欄を、看板の各レーン下部とテーブルの最下行に置く (Enter で追加、フォーカスは残す)。
  テーブルのクイック追加は現在の絞り込み条件を初期値にし、追加した行がそのまま見えるようにする。

### タスクの標準項目

- タイトル / サマリー / 概要 / 完了予定日 / 着手予定日 / 着手日 / 完了日。
- これに加えて レーン / 担当者 / 優先度 / マイルストーン / タグ / 更新履歴 を持つ。
- 各タスクに更新履歴 (いつ・誰が・何を変更したか) を自動記録する。操作者名は localStorage に保存する。
- 操作者はヘッダー右上にログインアイコン風 (アバター + 名前) で常時表示し、クリックでメンバー一覧から選べる。設定 › 基本 の項目と同期する。

### マスタ

- メンバー: 一覧で複数選択して一括削除、テキストエリアから一括追加できる UI を用意する。
  アイコン画像を設定できる (正方形に切り抜いて 64px に縮小した data URL として HTML 内に保存。未設定時は頭文字と色のアバター)。
  アイコンは「メンバー名.png」の ZIP または複数画像で一括設定できる (名前は空白・全角半角を無視して照合。不一致分は新規作成を選べる)。
  ZIP の読み込みは外部ライブラリを使わず DecompressionStream で行う。
- タグ: ラフに扱う。タスクの編集ダイアログやテーブルからその場で新規作成・付与できる。
- マイルストーン: 標準は「なし」(空)。必要に応じて設定から追加する。
- カスタム項目: プロジェクトごとに複数追加できる。型は text / long text / date / list。標準は「なし」(空)。
  タスク編集ダイアログでは標準項目と同じグリッドに並べて表示し、セクションを分けない。

### LIST (テーブル) View

- 1 タスク 1 行のコンパクト表示にする (行高を抑える)。タイトルとサマリーは別の列にする。
- **表示モード** と **編集モード** を切り分ける (ツールバーのトグル、既定は表示)。
  表示モードはテキスト表示のみで、行クリックで詳細ダイアログを開く。編集モードはタイトル・サマリーを含む全セルをインライン編集でき、
  # 列の ⤢ ボタンで詳細を開く。ツールバー (モード・ビュー・列・CSV) は横スクロールしても固定。
- 列ごとの絞り込み (フィルタ行) に対応する。
- 列の表示 / 非表示、絞り込み、並び順のセットを「ビュー」として名前を付けて保存できる (データファイルに保存)。

### 日付の表示形式

- 画面に表示する日付は必ず 0 埋めにする (短縮形は `MM/DD`、完全形は `YYYY/MM/DD`、日時は `YYYY/MM/DD HH:MM`)。
  データ上の日付は ISO 形式 `YYYY-MM-DD` で保持する。

### 設定モーダル

- 高さは固定 (タブを切り替えても高さが変わらないようにする)。

## 実装上の注意

- 単一ファイル構成を維持する。外部 CSS / JS / CDN への依存を追加しない。
- `<script id="backlog-script">` 内に `</script>` という文字列を直接書かない (HTML パーサが終了タグと解釈する)。
  文字列で必要な場合は `'<' + '/script>'` のように分割する。正規表現でも `<\/script` のように書く。
- 埋め込む JSON は `<` を `\u003c` にエスケープして出力する (`dataJson()` を経由する)。
- 新しい CSS ルールはすべて `.backlog-root { ... }` のネスト内に書き、`body` / `html` / `:root` を直接スタイルしない。
- `document.title` や `document.body` など、ホストページに影響するものは単体ファイル (`data-standalone`) の場合のみ操作する。
- DOM の取得は `root` (= `#backlog-app`) を基点にし、`document.getElementById` などページ全体への問い合わせを避ける。
- タスクを変更する処理は `snapshot()` → 変更 → `recordDiff()` の順で書き、更新履歴が残るようにする。
- データ構造を変えるときは `normalize()` で旧データからの移行 (既定値の補完) を行い、`DATA_VERSION` を上げる。
- `window.confirm` / `window.prompt` / `window.alert` は使わない (サンドボックス化された埋め込みやプレビューではブロックされ、
  ボタンが「動作しない」ように見える)。確認・入力は `askConfirm()` / `askPrompt()` (アプリ内 `<dialog>`、Promise を返す) を使う。
- 変更後は Playwright (Chromium) で動作確認する。手順は `README.md` の「動作確認」を参照。
